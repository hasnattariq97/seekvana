Implement Phase 2 — Design System from SEEKVANA-BUILD-GUIDE.md.

Read CLAUDE.md first for project conventions, then complete all 6 tasks in Phase 2:
1. Supabase client at src/lib/supabase.ts
2. Google Fonts (Fraunces, Inter, JetBrains Mono) in layout.tsx
3. Replace globals.css with full color token system (light + dark themes)
4. Update tailwind.config.ts with all custom color tokens and font families
5. ThemeProvider and ThemeToggle components
6. Root layout with fonts, ThemeProvider, and metadata

After all tasks: run npm run build and fix any errors before reporting done.
