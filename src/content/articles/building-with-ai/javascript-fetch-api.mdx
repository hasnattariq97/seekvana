---
title: "What Is Fetch in JavaScript? Calling an API Right"
description: "Fetch in JavaScript sends an HTTP request and returns a Promise, but a 404 or 500 won't trigger an error. Here's how to catch it with response.ok."
pillar: "building-with-ai"
slug: "javascript-fetch-api"
difficulty: "beginner"
readTime: 7
author: "Seekvana"
publishedAt: "2026-07-09"
tags: ["javascript fetch api", "what is fetch in javascript", "call an api javascript", "async await"]
featured: false
lessonModule: "06"
lessonNumber: "06.08"
faqs:
  - q: "Does fetch work the same way in Node.js as it does in the browser?"
    a: "Yes, as of Node.js 18 and later, fetch ships built in, no extra library needed. Older Node versions required a package like node-fetch to get the same behavior. If you're running a script and fetch is undefined, that's almost always an old Node version."
  - q: "Why do I need await twice, once for fetch and once for .json()?"
    a: "Because there are two separate asynchronous steps. The first await waits for the response headers and status to arrive. The second waits for the body to be read and parsed as JSON. Skipping the second await gives you a pending Promise instead of your actual data."
  - q: "Can I use fetch to call any API, or only some?"
    a: "You can call any API that allows requests from a browser, which comes down to a setting on the server called CORS. If a request fails with a CORS error in the console, that's the server refusing browser-based requests, not a mistake in your fetch call."
  - q: "What's the difference between fetch and axios?"
    a: "Fetch is built into every modern browser and Node.js, so it needs no installation. Axios is a separate library that automatically rejects on error status codes and has a few more convenience features, which is why some teams prefer it. For a beginner, fetch is the better starting point because you already have it."
---

{/* IMAGE PROMPT: Editorial flat illustration of a browser window sending a small paper airplane request across a gap to a server icon, receiving a labelled response envelope back, warm cream background (#FAF8F3), clay accent (#C9633F) on the paper airplane, clean and minimal, no text in image | FILENAME: cover.webp | DIMENSIONS: 1200x630 */}

<ArticleImage
  src="/images/articles/building-with-ai/javascript-fetch-api/cover.webp"
  alt="A browser window sending a request across to a server and receiving a response back, representing how fetch works in JavaScript"
  priority
  isCover
/>

You call `fetch()` for a list of users, nothing shows up on the page, and the console is silent. No red error, no `.catch()` firing, nothing to click. You open the Network tab you learned about last lesson and the request shows a status of 404.

Fetch never told you that. As far as your code was concerned, the request "worked."

`fetch()` is JavaScript's built-in way to send an HTTP request from your code and get a response back as a Promise. It's how a webpage calls an [API](/library/building-with-ai/what-is-an-api) without reloading the page. The part that catches almost every beginner: fetch only fails on a real network problem, never on a bad status code like 404 or 500. That's exactly what happened above.

> **Key Takeaways**
> - `fetch(url)` sends a request and returns a Promise you can `await`
> - `fetch()` does not reject on a 404 or 500, only on a genuine network failure
> - You must check `response.ok` yourself to catch a failed request
> - Reading the body needs a second `await`, with `response.json()`
> - A POST request needs a `method`, `headers`, and a `body` you build with `JSON.stringify()`

---

## What Is Fetch in JavaScript?

`fetch()` is a function built into every modern browser and into <a href="https://nodejs.org/en/blog/release/v18.0.0" target="_blank" rel="noopener noreferrer">Node.js 18 and later</a>. You give it a URL, it sends a <a href="https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API" target="_blank" rel="noopener noreferrer">request over the network</a>, and it returns a Promise that resolves once the response's headers and status arrive.

It's the direct code version of what you watched happen in your browser's [DevTools Network tab](/library/building-with-ai/browser-devtools) last lesson. Every request you saw listed there, your code can now send and read for itself.

## A Basic fetch() Request, Step by Step

Here's a `fetch()` call that gets a list of users from a public test API and logs the first one:

```javascript
async function getUsers() {
  const response = await fetch("https://jsonplaceholder.typicode.com/users");
  const users = await response.json();
  console.log(users[0]);
}

getUsers();
```

Two separate waits happen here. The first `await` pauses until the response's status and headers arrive. That's the moment you'd see the request finish in the Network tab. The second `await` waits for the body to be read and turned from text into a JavaScript array with `response.json()`.

Skip that second `await` and you'll log a pending Promise instead of your data. It's one of the most common first mistakes with fetch, and one I still catch myself making when I'm moving fast and forget the second wait exists.

## Why fetch() Doesn't Throw an Error on a 404

This is the part that breaks real projects silently. `fetch()` only rejects its Promise when the network itself fails, no internet connection, a broken DNS lookup, a request that never reaches a server. A 404 or a 500 still counts as a completed request as far as fetch is concerned, so your `.catch()` never fires.

<Warning>
If you skip checking `response.ok`, a wrong URL or a dead endpoint will fail completely silently: no error, no console message, just missing data on the page.
</Warning>

The fix is to check it yourself, right after the first `await`:

```javascript
async function getUser(id) {
  const response = await fetch(`https://jsonplaceholder.typicode.com/users/${id}`);
  if (!response.ok) {
    throw new Error(`Request failed with status ${response.status}`);
  }
  return await response.json();
}
```

`response.ok` is `true` for any status in the 200 range and `false` otherwise. Checking it before you touch `response.json()` turns a silent failure into an actual error you can catch and handle. That's exactly the kind of bug the [status codes](/library/building-with-ai/rest-apis-get-post) you learned about last time were designed to signal.

Fetch can also fail silently for a second reason: <a href="https://developer.mozilla.org/en-US/docs/Web/HTTP/Guides/CORS" target="_blank" rel="noopener noreferrer">CORS</a>, a browser security rule that blocks a request if the server hasn't explicitly allowed it. A CORS failure shows up as an error in the console, but it's easy to mistake for a bug in your own code instead of a setting on the server you're calling.

## Sending Data with fetch() (POST)

A GET request only needs a URL. A POST request needs to say what data you're sending and how it's formatted, which means three extra pieces:

```javascript
async function createUser(name, email) {
  const response = await fetch("https://jsonplaceholder.typicode.com/users", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ name, email }),
  });
  return await response.json();
}
```

`method` tells the server this is a POST, not the default GET. `headers` tells it the body is JSON, so it knows how to read it. `body` is where `JSON.stringify()` matters. Your data starts as a JavaScript object, but a request body has to travel as text. `JSON.stringify()` is what converts one to the other.

Forget `JSON.stringify()` and you'll send the literal text `[object Object]` instead of your actual data. That mistake produces no error either. It just leaves the server with something it can't use.

{/* IMAGE PROMPT: Editorial flat diagram titled "How fetch() Works" showing request/response flow between browser and server, a response.ok check turning silent failures into real errors, and a POST example with method/headers/body, warm cream background (#FAF8F3), clay accent (#C9633F), no extraneous text | FILENAME: how-fetch-works.webp | DIMENSIONS: 1600x950 */}

<ArticleImage
  src="/images/articles/building-with-ai/javascript-fetch-api/how-fetch-works.webp"
  alt="Diagram summarizing how fetch() works: sending a request, awaiting the response, checking response.ok before reading the body, and sending a POST request with method, headers, and body"
  caption="fetch() only rejects on network errors, a 404 or 500 still returns response.ok as false, which is why checking it before reading the body is what catches a silent failure."
/>

---

## Your Task

Time to see fetch in JavaScript handle both a working request and a silently broken one, in your own console.

<Steps>
  <Step number={1} title="Fetch a real user and log it">
    Open your browser console (or a `.js` file run with Node) and paste in the `getUsers()` function from the "Basic fetch() Request" section above, then call it.

    Confirm you see a real user object logged, not a pending Promise.
  </Step>
  <Step number={2} title="Deliberately break the URL and watch nothing happen">
    Change the URL inside the same function to a user id that doesn't exist, then run it again:

    ```javascript
    const response = await fetch("https://jsonplaceholder.typicode.com/users/9999");
    ```

    The server returns a 404, but there's no error and no red text. `console.log(users[0])` just logs `undefined`.
  </Step>
  <Step number={3} title="Add the response.ok check and see the difference">
    Add the `response.ok` check from the "Why fetch() Doesn't Throw an Error on a 404" section above, right after the `fetch()` line and before you touch `response.json()`.

    Run it again against the same broken URL and confirm you now see a real thrown error instead of silence.
  </Step>
</Steps>

<Note>
  Completed all three steps above? You've finished Lesson 06.08.
</Note>

<FAQ>
  <FAQItem question="Does fetch work the same way in Node.js as it does in the browser?">
    Yes, as of Node.js 18 and later, fetch ships built in, no extra library needed. Older Node versions required a package like node-fetch to get the same behavior. If you're running a script and fetch is undefined, that's almost always an old Node version.
  </FAQItem>
  <FAQItem question="Why do I need await twice, once for fetch and once for .json()?">
    Because there are two separate asynchronous steps. The first await waits for the response headers and status to arrive. The second waits for the body to be read and parsed as JSON. Skipping the second await gives you a pending Promise instead of your actual data.
  </FAQItem>
  <FAQItem question="Can I use fetch to call any API, or only some?">
    You can call any API that allows requests from a browser, which comes down to a setting on the server called CORS. If a request fails with a CORS error in the console, that's the server refusing browser-based requests, not a mistake in your fetch call.
  </FAQItem>
  <FAQItem question="What's the difference between fetch and axios?">
    Fetch is built into every modern browser and Node.js, so it needs no installation. Axios is a separate library that automatically rejects on error status codes and has a few more convenience features, which is why some teams prefer it. For a beginner, fetch is the better starting point because you already have it.
  </FAQItem>
</FAQ>
